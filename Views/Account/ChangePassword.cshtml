@{
    ViewBag.Title = "修改密碼";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-3">
            <div class="card mb-4">
                <div class="card-header">
                    <h4>帳戶選單</h4>
                </div>
                <div class="list-group list-group-flush">
                    <a href="@Url.Action("Profile", "Account")" class="list-group-item list-group-item-action">
                        <i class="bi bi-person-circle me-2"></i> 個人資料
                    </a>
                    <a href="@Url.Action("ChangePassword", "Account")" class="list-group-item list-group-item-action active">
                        <i class="bi bi-shield-lock me-2"></i> 修改密碼
                    </a>
                    <a href="@Url.Action("Index", "MyCollection")" class="list-group-item list-group-item-action">
                        <i class="bi bi-bookmark-star me-2"></i> 我的收藏
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-9">
            <div class="card">
                <div class="card-header">
                    <h3>修改密碼</h3>
                </div>
                <div class="card-body">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success">
                            @TempData["SuccessMessage"]
                        </div>
                    }

                    @if (TempData["ErrorMessage"] != null || ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger">
                            @(TempData["ErrorMessage"] ?? ViewBag.ErrorMessage)
                        </div>
                    }

                    <form method="post" action="@Url.Action("UpdatePassword", "Account")">
                        <div class="mb-3">
                            <label for="CurrentPassword" class="form-label">當前密碼</label>
                            <input type="password" class="form-control" id="CurrentPassword" name="CurrentPassword" required>
                            <div class="form-text">請輸入您當前的密碼以驗證身份</div>
                        </div>
                        <div class="mb-3">
                            <label for="NewPassword" class="form-label">新密碼</label>
                            <input type="password" class="form-control" id="NewPassword" name="NewPassword" required>
                            <div class="form-text">密碼應至少包含6個字符</div>
                        </div>
                        <div class="mb-3">
                            <label for="ConfirmPassword" class="form-label">確認新密碼</label>
                            <input type="password" class="form-control" id="ConfirmPassword" name="ConfirmPassword" required>
                            <div class="form-text">再次輸入新密碼以確認</div>
                        </div>
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-shield-lock me-1"></i> 更新密碼
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-header">
                    <h3>密碼安全提示</h3>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li>使用至少8個字符的密碼</li>
                        <li>包含大小寫字母、數字和特殊符號</li>
                        <li>避免使用容易猜測的個人信息（如生日）</li>
                        <li>定期更換密碼以提高帳戶安全性</li>
                        <li>不要與其他網站使用相同的密碼</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>